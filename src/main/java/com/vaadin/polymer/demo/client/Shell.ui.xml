<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'>

  <div>
    <!-- GSS does not parse CSS custom variables -->
    <style>
      app-header {
        color: var(--app-primary-color);
        background-color: var(--app-secondary-color);
      }
      app-drawer app-header-layout {
        background-color: var(--app-drawer-content-background-color);
      }

      app-drawer paper-menu {
        background-color: transparent !important;
      }

      .content {
        margin-top: 64px;
      }
    </style>

    <app-drawer-layout ui:field="app" class="fit">

      <app-drawer ui:field="drawer">
        <app-header-layout has-scrolling-region="true" >
          <app-header>
            <app-toolbar>Menu</app-toolbar>
          </app-header>

          <paper-listbox ui:field="menu">
            <paper-item>User List</paper-item>
            <paper-item>Videos</paper-item>
            <paper-item>Todos</paper-item>
          </paper-listbox>

        </app-header-layout>
      </app-drawer>

      <app-header-layout ui:field="main" attributes="fullbleed" has-scrolling-region="true" class="fit mright">
        <app-header>
          <app-toolbar>
            <paper-icon-button icon="menu" drawer-toggle="true"></paper-icon-button>
            <div main-title="" ui:field="title">
              MyApp
              <paper-badge icon="flight-takeoff" ui:field="offline"></paper-badge>
            </div>
          </app-toolbar>
        </app-header>

        <div ui:field="content" class="content fit layout vertical"></div>

      </app-header-layout>
    </app-drawer-layout>
  </div>
</ui:UiBinder>
